name: smart_leds_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.1
  http: ^1.2.0
  crypto: ^3.0.3
  path_provider: ^2.1.2
  file_selector: ^1.0.3
  async: ^2.11.0

  multicast_dns:
    git:
      url: https://github.com/jmojzes21/multicast_dns.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/patterns/
